{
  "name": "overlap-combos-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "tsc --noEmit",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "seed:sql": "node scripts/seed-recipes.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.43.0"
  }
}
